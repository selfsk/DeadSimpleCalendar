import XCTest
@testable import DeadSimpleCalendar

final class DeadSimpleCalendarTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DeadSimpleCalendar().text, "Hello, World!")
    }
}
